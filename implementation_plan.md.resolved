# 🔄 LVE Chain Swap & Pool Implementation

> **ВАЖНО ДЛЯ АГЕНТА**: Прочитай секцию "Правила работы" ПЕРЕД началом работы!

---

## 🚨 Правила работы с пользователем

### ❌ Что НЕ делать:
1. **НЕ делай изменения без плана** - сначала составь план и покажи пользователю
2. **НЕ ломай существующий код** - проверяй `npm run build` после КАЖДОГО изменения
3. **НЕ добавляй лишние зависимости** - используй существующие библиотеки
4. **НЕ игнорируй ошибки** - если билд сломался, исправь СРАЗУ
5. **НЕ делай слишком много изменений за раз** - маленькие коммиты лучше
6. **НЕ играй на нервах** - спрашивай если не уверен, не гадай

### ✅ Что ДЕЛАТЬ:
1. **Сначала изучи код** - используй grep_search, view_file, view_file_outline
2. **Составь план** - покажи что будешь менять ПЕРЕД изменениями
3. **Маленькие шаги** - одно изменение → build → test → commit
4. **Проверяй билд** - `npm run build` после каждого изменения
5. **Коммить часто** - `git add -A && git commit -m "..." && git push`
6. **Отвечай кратко** - не пиши простыни текста

### 📋 Перед началом работы ОБЯЗАТЕЛЬНО:
```bash
# 1. Проверь что билд работает
npm run build

# 2. Проверь статус git
git status

# 3. Посмотри структуру проекта
ls -la src/
```

---

## 📁 Архитектура проекта

### Структура директорий:
```
/home/abdulloh/my works/2026/my-blockchain/
├── src/
│   ├── node/              # CLI, API сервер
│   │   ├── api/routes/    # REST API endpoints
│   │   │   ├── staking.ts # Staking endpoints
│   │   │   ├── pool.ts    # Pool endpoints (ЧАСТИЧНО ГОТОВ)
│   │   │   └── ...
│   │   └── config.ts      # Конфигурация (pool config есть!)
│   ├── protocol/          # Blockchain core
│   │   ├── blockchain/    # Blockchain.ts, Block.ts, Transaction.ts
│   │   └── storage/       # Сохранение данных
│   ├── runtime/           # Runtime модули
│   │   ├── staking/       # StakingPool, BlockProducer
│   │   └── pool/          # Pool модули (УЖЕ ЕСТЬ!)
│   │       ├── LiquidityPool.ts      # AMM логика
│   │       ├── PoolStateManager.ts   # Состояние пула
│   │       ├── poolModule.ts         # Обработка транзакций
│   │       ├── TWAPOracle.ts         # Ценовой оракул
│   │       └── USDTBalanceManager.ts # Баланс USDT
│   └── network/           # P2P сеть
├── frontend/              # React frontend (Vite)
│   └── src/pages/         # Страницы приложения
├── runners/               # Скрипты запуска нод
└── data/                  # Данные (testnet/mainnet)
```

### Существующие pool файлы:
| Файл | Описание | Статус |
|------|----------|--------|
| [LiquidityPool.ts](file:///home/abdulloh/my%20works/2026/my-blockchain/src/runtime/pool/LiquidityPool.ts) | AMM x*y=k логика | ✅ Готов |
| [PoolStateManager.ts](file:///home/abdulloh/my%20works/2026/my-blockchain/src/runtime/pool/PoolStateManager.ts) | Управление состоянием | ✅ Готов |
| [poolModule.ts](file:///home/abdulloh/my%20works/2026/my-blockchain/src/runtime/pool/poolModule.ts) | Обработка транзакций | ✅ Готов |
| [TWAPOracle.ts](file:///home/abdulloh/my%20works/2026/my-blockchain/src/runtime/pool/TWAPOracle.ts) | TWAP ценовой оракул | ✅ Готов |
| [pool.ts](file:///home/abdulloh/my%20works/2026/my-blockchain/src/node/api/routes/pool.ts) (API routes) | REST endpoints | ⚠️ Нужно проверить |

### Конфигурация пула (config.ts):
```typescript
pool: {
    initialLVE: 100_000,     // 100K LVE
    initialUSDT: 5_000,      // 5K USDT
    feePercent: 0.3,         // 0.3% swap fee
    burnPercent: 30,         // 30% of fees burned
}
```

---

## 🎯 Текущий статус Pool

### Бэкенд (Node):
- [x] LiquidityPool.ts - AMM логика
- [x] PoolStateManager.ts - состояние
- [x] poolModule.ts - обработка транзакций
- [x] API routes /api/pool/* - частично готовы
- [ ] **ПРОВЕРИТЬ**: работают ли swap транзакции
- [ ] **ПРОВЕРИТЬ**: сохраняется ли состояние пула

### Фронтенд:
- [ ] Страница Swap (UI)
- [ ] Подключение к API
- [ ] Transaction signing для swap

---

## 📝 План реализации Swap UI

### Шаг 1: Проверка бэкенда
```bash
# Проверить API endpoints
curl http://localhost:3001/api/pool/info
curl http://localhost:3001/api/pool/price
```

### Шаг 2: Создать страницу Swap
**Файл**: [frontend/src/pages/Swap.tsx](file:///home/abdulloh/my%20works/2026/my-blockchain/frontend/src/pages/Swap.tsx)

```
┌─────────────────────────────────┐
│         Swap LVE ↔ USDT         │
├─────────────────────────────────┤
│  From: [100    ] LVE    ▼       │
│  ─────────────────────────      │
│  To:   [~490   ] USDT   ▼       │
├─────────────────────────────────┤
│  Rate: 1 LVE = 4.9 USDT         │
│  Fee: 0.3%                      │
│  Slippage: 0.5%                 │
├─────────────────────────────────┤
│          [ SWAP ]               │
└─────────────────────────────────┘
```

### Шаг 3: API интеграция
- GET `/api/pool/info` - информация о пуле
- GET `/api/pool/price` - текущая цена
- POST `/api/pool/swap` - выполнить swap (подписанная транзакция)

### Шаг 4: Transaction signing
Swap = подписанная транзакция с типом `SWAP_LVE_TO_USDT` или `SWAP_USDT_TO_LVE`

---

## 🔧 Команды для разработки

```bash
# Backend
cd /home/abdulloh/my\ works/2026/my-blockchain
npm run build              # Компиляция TypeScript
npm run dev                # Запуск в dev режиме

# Frontend
cd frontend
npm run dev                # Vite dev server
npm run build              # Production build

# Node
./runners/validator/start.sh  # Запуск валидатора

# Git
git add -A && git commit -m "feat: ..." && git push
```

---

## ⚠️ Важные особенности

### 1. Transaction Types
```typescript
type TransactionType = 
    | 'TRANSFER' 
    | 'STAKE' | 'UNSTAKE' 
    | 'DELEGATE' | 'UNDELEGATE' 
    | 'COMMISSION'
    | 'SWAP'        // <- для swap
    | 'ADD_LIQUIDITY' | 'REMOVE_LIQUIDITY';
```

### 2. Staking Config (недавно добавлено)
```typescript
staking: {
    minValidatorStake: 100,
    maxConcentration: 33,    // Max 33% на валидатора
    minCommission: 0,
    maxCommission: 30,
}
```

### 3. Reward транзакции
Валидаторы получают reward транзакции после создания блока. Исправлено недавно.

### 4. blocksCreated counter
Подсчитывается в [rebuildFromChain()](file:///home/abdulloh/my%20works/2026/my-blockchain/src/runtime/staking/StakingPool.ts#823-867) при перезагрузке.

---

## ✅ Чеклист перед работой

- [ ] `npm run build` работает без ошибок
- [ ] `git status` чистый (нет uncommitted changes)
- [ ] Прочитал существующий pool код
- [ ] Понял архитектуру проекта
- [ ] Знаю какие файлы буду менять
- [ ] Составил план и показал пользователю

---

## 📞 Контакты и ресурсы

- **Репозиторий**: https://github.com/abdulloh5007/lvenc-blockchain
- **VPS Seed Node**: wss://seed1.lvenc.site
- **Testnet API**: порт 3001
- **P2P Port**: 6001

---

*Последнее обновление: 2026-01-30*
